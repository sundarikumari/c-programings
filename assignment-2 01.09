#include <stdio.h>

int fact(int);
int main()
{
    int n,res;
    printf("Enter a number: ");
    scanf("%d",&n);
    res=fact(n);
    printf("%d",res);
    return 0;
}

int fact(int x)
{
    if(x==1)
    return 1;
    else
    return x*fact(x-1);
}
